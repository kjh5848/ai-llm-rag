
# Part 7. ex02/ex03 서버 분리 및 안정화
## 개요
- 기존 를 으로 복제하고, (Hybrid Search)와 (Agentic RAG)을 각각 독립적인 서버로 분리했습니다.
- 에서 발견된 UI/UX 문제(에이전트 모드 노출, 버튼 스타일)와 설정 오류(DB 연결)를 수정했습니다.

## 주요 변경 사항
### ex02 (Hybrid Search)
1. **에이전트 모드 UI 제거**: 템플릿 경로 문제( 오류)로 인해  화면이 보이던 현상을 해결하고, 순수 검색 모드로 복원했습니다.
2. **UI 일관성 확보**: '내역 지우기' 버튼 스타일을 과 통일하고, 근거 표시 텍스트를 로 변경했습니다.
3. **DB 연결 안정화**: 의  설정을 로 변경하여  연결 오류를 해결했습니다.
4. **독립 서버 구성**: 포트 을 사용하며, (8001)과 동시에 실행 가능합니다.

### ex03 (Agentic RAG)
1. **서버 복제**: 의 최신 상태를 으로 복제하여 독립적인 실습 환경을 구축했습니다.
2. **포트 변경**: 포트 을 사용하여 와의 포트 충돌을 방지했습니다.

## 실행 방법
- **ex02 (구 버전)**: 
- **ex03 (신 버전)**: 
